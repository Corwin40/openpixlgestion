{% extends 'base.html.twig' %}

{% block title %}New Service{% endblock %}

{% block body %}
    <h1>Crée un nouveau service</h1>

    {% block content%}
    <section>
        <div class="container-fluid">
            <div class="col-xs-12 col-sm-12 col-md-12 col-lg-10 col-xl-10">
                <div class="card">
                    {{ include('admin/service/_form.html.twig') }}
                </div>
            </div>
        </div>
    </section>

        <div id="bouton_form">
            <button class="btn btn-sm btn-outline-success">{{ button_label|default('Enregistrer') }}</button>
        </div>
    {% endblock %}

    <a href="{{ path('app_admin_statut_index') }}">Retour à la liste</a>
{% endblock %}
